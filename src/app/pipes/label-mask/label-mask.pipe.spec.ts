import { LabelMaskPipe } from './label-mask.pipe';

describe('LabelMaskPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelMaskPipe();
    expect(pipe).toBeTruthy();
  });
});
