import { IconInterpreterPipe } from './icon-interpreter.pipe';

describe('IconInterpreterPipe', () => {
  it('create an instance', () => {
    const pipe = new IconInterpreterPipe();
    expect(pipe).toBeTruthy();
  });
});
